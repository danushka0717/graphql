
type Book {
    isbn: String
    title: String
    publisher: String
    authors: [String]
    publishDate: String
}

type Query {
    allBooks: [Book]
    book(id: String): Book
}

schema {
    query: Query
}